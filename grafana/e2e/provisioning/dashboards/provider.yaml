# Provision the shipped dashboard (src/dashboards, mounted to /var/lib/grafana/dashboards)
# so the E2E can open "HTCondorDB Overview" and assert it renders.
apiVersion: 1
providers:
  - name: htcondordb
    type: file
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
